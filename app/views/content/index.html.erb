
<main>
<% @content.each do |content| %>
  <div class="row">
    <div class="col s8 offset-s2">
      <div class="card">
        <div class="card-content">

            <p><%= content.content %></p>

        </div>
        <div class="card-action">
            <a href="#">SHOW</a>
            <a href="#">EDIT</a>
            <a href="#">DESTROY</a>
        </div>
      </div>
    </div>
  </div>
<% end %>
</main>
  <%= paginate(@content) %>
